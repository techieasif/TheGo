{{template "base" .}}
   {{define "content"}}
      <div class="container">
          <div class="row">
               <div class="col">
                    <h1>This is the about page</h1>
                    <h2>This is passed name: {{index .StringMap "name"}}</h2>
               </div>
          </div>
      </div>
{{end}}